# Generate MySQL DB Tables Summary

## Usage

```sh
    # All tables
    py sales_service_tables_summary.py --tables --all

    # Specific tables (comma-separated)
    py sales_service_tables_summary.py --tables users,orders,products

    # Specific tables (space-separated)
    py sales_service_tables_summary.py --tables users orders products
```
