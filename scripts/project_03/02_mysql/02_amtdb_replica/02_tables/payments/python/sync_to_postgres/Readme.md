# Sync Payments from MySQL to PostgreSQL

# Usage

# Install dependencies

pip install mysql-connector-python psycopg2-binary python-dotenv

# Run sync

python script.py

# Verify only

python script.py --verify-only

# Custom batch size

python script.py --batch-size 5000
