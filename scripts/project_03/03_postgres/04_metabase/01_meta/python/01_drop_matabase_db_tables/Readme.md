# Drop PostgreSQL Database Tables

```sh
    # Drop specific table(s):
    py drop_postgres_db_tables.py --tables customers
    py drop_postgres_db_tables.py --tables customers account installment_schedules

    # Drop ALL configured tables:
    py drop_postgres_db_tables.py --tables all

    # Interactive mode (no arguments):
    py drop_postgres_db_tables.py
```
